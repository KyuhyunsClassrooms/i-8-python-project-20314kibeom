# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제:내가 만들 프로그램의 주제:스마트 용돈 사용 경고기 및 소비 분석기

이 프로그램을 만든 이유:평소에 용돈 관리가 어려워서, 카테고리별로 지출액을 계산하고 예산이 초과되면 경고를 해주는 프로그램이 필요했기 때문입니다."


---

## 2. 문제 분해 및 모델링

### 입력
이 프로그램은 정확한 소비 분석을 위해 사용자로부터 1) 카테고리별 목표 OOO(정수), 2) 소비 항목의 OO(문자열), 3) 지출 OO(정수), 4) 해당 항목이 속하는 OOO(문자열) 데이터를 입력받습니다

### 처리
데이터 저장: 입력받은 소비 내역(항목명, 금액, 카테고리)을 2차원 리스트인 history에 하나의 O으로 추가하여 모델링합니다.
합계 계산: OO문을 사용하여 history 리스트를 순회하며, 카테고리별로 누적 지출액을 합산합니다.
예산 비교: OO문을 사용하여 카테고리별 누적 지출액이 설정한 목표 예산을 초과했는지 판정합니다.

### 출력
"분석이 완료되면 프로그램은 1) 현재까지 누적된 전체 OOOO 리스트, 2) 카테고리별 OOOO 및 남은 금액, 3) 예산을 초과했을 경우 화면에 띄워주는 특별 OO 메시지를 출력합니다."


---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름:history

한 행의 의미:사용자가 한 번 지출한 '하나의 소비 내역'을 의미합니다.

각 열의 의미:

| 열 번호 | 의미 | 실제 예시 |
|---:|---|---|
| 0번 열 | 소비 항목의 이름 (문자열) | "떡볶이" |
| 1번 열 | 지출한 금액 (정수) | 2500 |
| 2번 열 | 지출 카테고리 (문자열) | "식비" |

이 2차원 리스트를 사용한 이유:여러 개의 지출 내역을 다룰 때, 소비 항목의 이름, 금액, 카테고리라는 서로 관련 있는 세 가지 정보를 뿔뿔이 흩어지게 두지 않고 하나의 행으로 묶어서 표처럼 관리하기 위해서입니다. 이렇게 2차원 리스트로 묶어두면 나중에 반복문(for문)을 사용해 특정 카테고리의 지출 금액만 골라서 합산하기가 매우 편리해집니다.


---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| show_menu() | 사용자에게 프로그램 메인 메뉴(1.입력 2.분석 3.종료)를 화면에 보여줌 | 없음 | 메뉴 텍스트를 화면에 출력 |
| add_expense() | 항목, 금액, 카테고리를 입력받아 2차원 리스트에 새로운 행으로 추가함 | history (2차원 리스트) | 2차원 리스트에 데이터 추가 완료 메시지 출력 |
| analyze_expenses() | 2차원 리스트를 순회하며 카테고리별 합계를 구하고, 예산 초과 여부를 경고함 | history, budgets (예산 데이터) | 지출 누적액, 남은 예산, 경고 메시지 출력 |

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름:analyze_expenses()

설명:이 함수는 프로그램의 핵심인 소비 분석을 담당합니다. 반복문(for문)을 사용하여 2차원 리스트인 history의 모든 행을 하나씩 꺼내어 카테고리별로 지출 금액을 합산합니다. 그 후 조건문(if문)을 사용하여 합산된 금액이 처음에 설정한 목표 예산을 넘었는지 비교하고, 만약 넘었다면 사용자에게 경고 메시지를 출력해 줍니다.


---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?
1. 사용자가 메인 메뉴에서 1번(입력), 2번(분석), 3번(종료) 중 어떤 번호를 선택했는지 검사할 때 if-elif문을 사용했습니다.
2. 소비 분석 기능에서 특정 카테고리의 누적 지출액이 처음에 설정한 '목표 예산'을 초과했는지(지출액 > 예산) 비교하여 경고 메시지를 띄울지 말지 결정할 때 사용했습니다.

### 반복문 사용 부분

무엇을 반복 처리했는가?
1. 사용자가 '종료'를 선택하기 전까지 프로그램 메인 메뉴를 계속 화면에 띄워주기 위해 while문을 사용했습니다.
2. 전체 소비 합계를 구하기 위해 for문을 사용하여 2차원 리스트인 history에 저장된 모든 지출 내역(행)을 처음부터 끝까지 하나씩 꺼내는 작업을 반복했습니다.

### 조건문과 반복문이 함께 작동한 부분
가장 핵심 기능인 analyze_expenses() 함수 안에서 함께 작동했습니다. for문으로 2차원 리스트(history)를 순회하면서 지출 내역을 하나씩 꺼내고, 그 안에서 if문을 사용해 "꺼낸 지출 내역의 카테고리가 '식비'인가? '문화'인가? '쇼핑'인가?"를 검사하여 각각 알맞은 곳에 금액을 더해주는 방식으로 작동했습니다.


---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```text

```

결과 분석:


---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력:

예상 결과:

실제 결과:


### 테스트 2: 예외 또는 경계 사례

입력:

예상 결과:

실제 결과:


### 발견한 오류와 수정한 방법


---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:


최종 코드에서 내가 직접 판단하거나 수정한 부분:


---

## 9. 한계와 개선 방안

현재 프로그램의 한계:


다시 개선한다면 추가하고 싶은 기능:


---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
